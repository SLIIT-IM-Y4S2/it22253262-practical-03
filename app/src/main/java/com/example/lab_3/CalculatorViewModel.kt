package com.example.lab_3

import androidx.lifecycle.ViewModel

class CalculatorViewModel : ViewModel() {
    // This will hold the final answer to share between fragments
    var finalResult: Double = 0.0
}